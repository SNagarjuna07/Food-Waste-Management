# Food-Waste-Management
A Spring Boot backend for managing food waste donations with JWT authentication, Redis-based rate limiting, MongoDB storage, Kafka notifications, and production-grade configuration using environment variables and profiles.
